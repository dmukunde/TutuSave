// Placeholder until the Supabase project exists and migrations are applied.
// Regenerate with:
//   npx supabase gen types typescript --project-id <project-id> > src/types/database.ts
export type Database = Record<string, never>;
